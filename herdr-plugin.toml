id = "jump-to-agent"
name = "Jump to Agent"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Jump to the first done/idle agent"
platforms = ["macos"]

[[actions]]
id = "jump"
title = "Jump to Done"
contexts = ["pane", "workspace", "tab"]
command = ["bash", "jump.sh"]

[[actions]]
id = "jump_not_done"
title = "Jump to Not Done"
contexts = ["pane", "workspace", "tab"]
command = ["bash", "jump_not_done.sh"]
